// Directors are now stored in the `directors` database table.
// Use createClient().from("directors").select("name").eq("status", "active") to fetch them.
// See: supabase/migrations/20260210000006_create_directors_table.sql
